#include "trainRunner.h"
#include "train.h"

Train::Train(Station stations[], int numStations) 
{
     
} // Train()

void Train::run(const Car cars[], int numCars, Action actions[], 
  int *numActions)
{
  *numActions = 0;
}  // run()
